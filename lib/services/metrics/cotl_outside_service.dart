class CotlOutsideService {
  /// TODO: รับรูป + mask แล้วคำนวณสัดส่วนระบาย "นอกเส้น" ต่อสีทั้งหมด (0..1)
  double computeCotl({
    required List<int> imageBytes,
    required List<int> maskBytes,
  }) {
    return 0.0;
  }
}
