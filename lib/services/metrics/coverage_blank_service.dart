class CoverageBlankService {
  /// TODO: รับรูป + mask แล้วคำนวณสัดส่วนว่างในเส้น (0..1)
  double computeBlankInside({
    required List<int> imageBytes,
    required List<int> maskBytes, // ขาว=พื้นที่ในเส้น
  }) {
    return 0.0;
  }
}
